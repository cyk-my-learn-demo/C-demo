1、公有成员是接口
2、私有成员 即不想开放给用户的接口。
3、保护成员，给派生类提供的成员

基类的私有成员被继承，但是不能直接访问
派生类不能继承构造函数、析构函数、赋值运算符、有元
当派生类没有重新定义方法或使用作用与解析运算符，才会调用积累方法

隐藏：子类对象优先考虑子类域自身成员（成员变量和成员函数）
覆盖：虚函数，成员函数类型一摸一样，父类指针调用子类对象成员
	覆盖只发生在有虚函数的情况下，且父子类成员函数类型必须一摸一样，即参数和返回类型都必须一致。

